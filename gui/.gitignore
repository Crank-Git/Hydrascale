# Wails build output
build/bin/

# Generated Wails JS bindings (regenerated by `wails build`/`wails dev`)
frontend/wailsjs/

# Node (unused — frontend is static — but keep it out just in case)
node_modules/

# Stray binary from `go build ./...` (use `wails build` instead)
/hydrascale-gui

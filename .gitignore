node_modules/                                                        
__pycache__/                                                         
*.pyc                                                                
.env                                                                 
dist/                                                                
build/                                                               
.pytest_cache/                                                       
.vscode/settings.json  # Keep settings out, use local configs in src/
.idea/                                                               
.DS_Store                                                            
mypy_cache/
htmlcov/
.omc/
.openagent
.openagent/
.sisyphus/
HYPERPLAN.md
TODOS.md
test-config.yaml
config.yaml
.hydrascale.lock
.worktrees/
/vendor/
.gstack/

# Wails GUI subproject: its frontend/dist (the actual frontend source) and
# build/ (icons + platform plists Wails needs) must be tracked despite the
# global dist/ and build/ ignores above. Only the compiled output is ignored.
!gui/frontend/dist/
!gui/build/
gui/build/bin/

# Claude Code
.claude/settings.local.json
.claude/worktrees/
.claude/agent-memory-local/
CLAUDE.local.md
.claude.local.md
.issue-flow.local.json

# Build output. The daemon binary must never be committed.
/hydrascale

# Private design source. Only the assets the console uses are committed, under
# internal/ui/static/brand/ (see docs/specs/features/01-desktop-client-removal.md).
branding/
